{
  "$schema": "https://json.schemastore.org/huskyrc",
  "hooks": {
    "pre-commit": "lint-staged && npm run type-check",
    "commit-msg": "commitlint -E HUSKY_GIT_PARAMS",
    "pre-push": "npm run test:unit && npm run security:audit && npm run build",
    "post-commit": "echo '✅ Commit successful! Running post-commit checks...' && npm run test:quick",
    "post-merge": "npm install && npm run bootstrap",
    "post-checkout": "if [[ $HUSKY_GIT_PARAMS =~ 1$ ]]; then npm install && npm run bootstrap; fi",
    "prepare-commit-msg": "exec < /dev/tty && git cz --hook || true"
  }
}